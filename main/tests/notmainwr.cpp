#include "globals.h"
#include "application.h"
#include "framebuffer.h"


/*
int main(){
    
    printf("Program starts...");
    readInput();

    bool breakloop = true;

    while(breakloop){
        for (int i = 0; i < TFT_WIDTH; i++)
        {
            for (int j = 0; j < TFT_HEIGHT; j++)
            {
                fb_drawPixel(i,j,TFT_BLACK);
            }
            
        }

        for (int i = 0; i < TFT_HEIGHT; i+=10)
        {
            fb_drawRectangle(10,i,30,40,0x001F);
        }
        
        fb_push_tft();

        if (isKeyPressed(KEY_A))
        {
            breakloop = false;
        }
        
    }

    printf("Program starts...");

}
*/
